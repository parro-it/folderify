'use strict';

var expect = require("expect.js");
var folderify = require("../lib/folderify");


describe("folderify", function () {
    it("is defined", function () {
        expect(folderify).to.be.an('object');
    });
});
